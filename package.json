{
  "name": "atsen",
  "version": "1.0.0",
  "type": "module",
  "description": "ATSEN - Educational Platform",
  "main": "backend/src/server.js",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install",
    "build:clean": "node -e \"const fs = require('fs'); const path = require('path'); const dir = path.join(__dirname, 'backend', 'public', 'assets'); if (fs.existsSync(dir)) fs.rmSync(dir, {recursive: true, force: true}); const html = path.join(__dirname, 'backend', 'public', 'index.html'); if (fs.existsSync(html)) fs.unlinkSync(html);\" && npm run build",
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev:all",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": [
    "education",
    "platform",
    "learning"
  ],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "axios": "^1.11.0",
    "bcryptjs": "^3.0.2",
    "cheerio": "^1.1.2",
    "jsonwebtoken": "^9.0.2",
    "lodash.debounce": "^4.0.8",
    "slugify": "^1.6.6",
    "vite": "^7.1.4"
  }
}
